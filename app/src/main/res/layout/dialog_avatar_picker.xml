<?xml version="1.0" encoding="utf-8"?>
<!--
    dialog_avatar_picker.xml — Диалог выбора аватарки
    Показывается по клику на аватар в ProfileActivity
    Лесные/мифические персонажи в виде сетки
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_dialog_rounded"
    android:padding="20dp">

    <!-- Заголовок диалога -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:text="Выбери своего персонажа"
        android:textColor="#222222"
        android:textSize="17sp"
        android:textStyle="bold"
        android:gravity="center" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Лесные духи и мифические существа 🌿"
        android:textColor="#636E72"
        android:textSize="13sp"
        android:gravity="center" />

    <!-- Сетка аватарок: 4 колонки -->
    <GridView
        android:id="@+id/gridAvatars"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:numColumns="4"
        android:horizontalSpacing="10dp"
        android:verticalSpacing="10dp"
        android:stretchMode="columnWidth"
        android:gravity="center" />

</LinearLayout>
